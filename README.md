# geocam-website-pages

Static build output of [geocamxyz/GeoCam-Website](https://github.com/geocamxyz/GeoCam-Website),
auto-published to GitHub Pages for the `www_staging.geocam.xyz` staging domain.

This repo contains only static HTML/CSS/JS artifacts. Source code lives in the
private source repo above. Do not edit files here manually — they are
regenerated from the source repo on every publish.

Custom domain (CNAME file): `www_staging.geocam.xyz`
